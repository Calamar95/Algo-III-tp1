#include <iostream>
#include <utility>
#include <vector>
#include <string>
#include <set>
#include <sstream>
#include <fstream>

using namespace std;

int main() {	
	/*
	vector<int> vec;
    ifstream in("Tp1Ej2.in");
    while (in.good()){
    	string st;
	    getline(in, st);
	    istringstream iss;
	    iss.str(st);
	    iss.clear();
	    int a;
	    while(iss>>a){
	    	vec.push_back(a);
	    }

    }
    */
    ofstream out;
    out.open("prueba.out");
    out << "probando" << endl;
    out.close();
  
   
    
    return 0;
    

}
